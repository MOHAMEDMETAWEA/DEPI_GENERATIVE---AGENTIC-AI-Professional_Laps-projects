-- Database: hospital_depi

-- DROP DATABASE IF EXISTS hospital_depi;

CREATE DATABASE hospital_depi
    WITH
    OWNER = postgres
    ENCODING = 'UTF8'
    LC_COLLATE = 'English_United States.1252'
    LC_CTYPE = 'English_United States.1252'
    LOCALE_PROVIDER = 'libc'
    TABLESPACE = pg_default
    CONNECTION LIMIT = -1
    IS_TEMPLATE = False;

--  creation of database 
create database hospital ; 
use hospital;

--DDL data definition language 
create table departments (
department_id int auto_increment primary key,
name varchar(50) unique 
)
		-- one department >> many docotrs 
create table doctors(
doctor_id int auto_increment primary key,
name varchar(100),
specilaity varchar(50),
department_id int ,
foreign key (department_id) references departments(department_id)
)

create table patients ( 
patient_id int auto_increment primary key , 
name varchar(50),
birth_date date,
gender varchar(10)
)
--1 patient >> many appointments
-- 1 doctor >> many appointment 
create table appointments(
appointment_id int auto_increment primary key ,
patient_id int,
doctor_id int , 
foreign key patient_id references  patients(patient_id)
foreign key doctor_id references  doctors(doctor_id)
reason text,
appointment_date date
)

-- one to many 
-- customer > order 

---many to one 
-- many  employess > one department

--many to many 
-- students >> courses 
--courses >> students

-- 1 apoointment >> many treatments 
create table treatments (
treatment_id int auto_increment primary key , 
appointment_id int,
foreign key (appointment_id) references appointments(appointment_id)
description text 
cost numeric(8,2) 
)

create table nurse ( 
nurse_id serial primary key,
name varchar(100),
shift varchar (20)
)
-- department / doctor / appoitment / nurse / treatment / patients


insert into nurse (name,shift)
values 
('Nour Hassan', 'Morning'),
('Ahmed Tarek', 'Night'),
('Sara Adel', 'Morning');
select * from nurse
truncate table nurse
drop table nurse  -- delete 
alter table nurse auto_increment=1

select * from appointments

-- changing column type 
alter table appoitments 
modify reason varchar(500)

select * from patients

alter table patients
rename column birth_date to date_of_birth

select * from treatments
alter table treatments
drop column cost

alter table treatments
add column cost numeric(8,2)

select * from nurse

update nurse 
set name= NULL

INSERT INTO Departments (name) VALUES 
('Cardiology'),
('Neurology'),
('Pediatrics'),
('Orthopedics');

select * from departments

select * from doctors

INSERT INTO Doctors (name, specilaity, department_id) VALUES 
('Dr. Amr Tarek', 'Cardiologist', 1),
('Dr. Rana Mostafa', 'Neurologist', 2),
('Dr. Hossam Youssef', 'Pediatrician', 3),
('Dr. Salma Farouk', 'Orthopedic Surgeon', 4);

INSERT INTO Patients (name, date_of_birth, gender) VALUES
('Ali Ahmed', '1990-01-15', 'Male'),
('Mona Samir', '1985-06-30', 'Female'),
('Omar Tarek', '2000-09-12', 'Male'),
('Layla Ibrahim', '1995-03-22', 'Female');

select * from Patients

INSERT INTO Appointments (patient_id, doctor_id, appointment_date, reason) VALUES
(1, 1, '2025-07-04', 'Chest pain'),
(2, 2, '2025-07-04', 'Headache'),
(3, 3, '2025-07-05', 'Flu symptoms'),
(4, 4, '2025-07-06', 'Back pain');

select * from Appointments


INSERT INTO Treatments (appointment_id, description, cost) VALUES
(1, 'ECG test and medication', 300.00),
(2, 'MRI and prescription', 1200.00),
(3, 'Cold medicine', 150.00),
(4, 'X-ray and physiotherapy', 800.00);

select * from treatments


select * from doctors
update doctors 
set specilaity ='chief neuro'
where doctor_id=2

update patients 
set name='omar adel'
where patient_id=3

select * from treatments 
update treatments
set description = ' mri only '
where appointment_id=2

select * from nurse
delete from nurse 
where nurse_id=3

alter table patients
add column phone varchar(20)

select * from patients

rename table nurse to head_nurses

alter table nurse 
rename to head_nurse

alter table doctors 
alter column name set not null

select * from treatments
update treatments 
set cost = cost *1.10
where cost > 700


select * from head_nurse 

update head_nurse 
set shift = 'day'


-- create >> create table / database 
-- alter >> changes in table structure 
-- drop >> delete column / table 
-- delete >> delete row 
-- update >> change in rows of table 
-- truncate >> keep structure but empty rows 
-- erd > entity relationship diagram 
-- relationships ( 1:M) ( M:1) ( 1:1 )



